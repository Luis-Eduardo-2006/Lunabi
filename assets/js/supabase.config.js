/* ===== LÜNABI — Configuración de Supabase =====
 *
 * Pega aquí tu Project URL y anon key de Supabase (Project Settings → API).
 * Si ambos valores están vacíos, el sitio seguirá funcionando en modo local
 * (localStorage) sin ningún error. Es seguro commitear este archivo con la
 * anon key — tus políticas RLS son las que protegen los datos.
 */
window.LUNABI_SUPABASE = {
  url:     '',   // ej. 'https://abcdxxxx.supabase.co'
  anonKey: ''    // ej. 'eyJhbGciOiJIUzI1NiIs...'
};

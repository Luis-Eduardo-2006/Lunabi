<div class="bg-scene">
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
</div>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg fixed-top navbar-glass py-2" id="mainNav">
  <div class="container">
    <a class="navbar-brand" href="#">Lumière</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link active" href="#hero">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="#features">Features</a></li>
        <li class="nav-item"><a class="nav-link" href="#code">Code</a></li>
        <li class="nav-item"><a class="nav-link" href="#cards">Showcase</a></li>
        <li class="nav-item"><a class="nav-link" href="#testimonials">Testimonials</a></li>
      </ul>
      <a href="#" class="btn btn-glass">Get Started</a>
    </div>
  </div>
</nav>

<div class="content-wrapper position-relative">

  <!-- Hero -->
  <section class="hero min-vh-100 d-flex align-items-center justify-content-center text-center" id="hero">
    <div class="container">
      <div class="fade-up">
        <div class="headline-glass-wrap d-flex justify-content-center mb-3" style="perspective:700px;">
          <div class="headline-glass tilt-3d">
            <h1 class="display-headline">The Art of<br><span class="grad">Glass&thinsp;morphism</span></h1>
          </div>
        </div>
        <p class="hero-sub mx-auto fw-light">A frosted-glass effect using <code style="color:var(--accent);font-size:0.95em">backdrop-filter</code>, translucent backgrounds, and subtle borders — demonstrated on a Bootstrap&nbsp;5 fixed navbar.</p>
        <a href="#code" class="btn btn-glass">View the CSS &darr;</a>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="section" id="features">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="section-title">Key Properties</h2>
        <p class="section-subtitle fw-light mt-2 mb-5">Three CSS pillars that make glassmorphism work</p>
      </div>
      <div class="row g-4">
        <div class="col-md-4 fade-up">
          <div class="glass-card">
            <div class="card-icon cyan d-flex align-items-center justify-content-center mb-4">◈</div>
            <h5 class="mb-2">backdrop-filter: blur()</h5>
            <p class="fw-light lh-lg">The core of the effect. Applies a Gaussian blur to everything behind the element, creating the frosted-glass appearance. Pair with <code>saturate()</code> to boost vibrancy.</p>
          </div>
        </div>
        <div class="col-md-4 fade-up">
          <div class="glass-card">
            <div class="card-icon pink d-flex align-items-center justify-content-center mb-4">◇</div>
            <h5 class="mb-2">Semi-transparent Background</h5>
            <p class="fw-light lh-lg">Use <code>rgba()</code> with low alpha values so the blurred content shows through. Values between 0.08–0.2 work best depending on your scene's contrast.</p>
          </div>
        </div>
        <div class="col-md-4 fade-up">
          <div class="glass-card">
            <div class="card-icon purple d-flex align-items-center justify-content-center mb-4">▣</div>
            <h5 class="mb-2">Subtle Border &amp; Shadow</h5>
            <p class="fw-light lh-lg">A thin translucent border and a soft box-shadow add depth and separation, making the glass pane feel like a real physical layer floating above the content.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Code -->
  <section class="section" id="code">
    <div class="container">
      <div class="row align-items-center g-5">
        <div class="col-lg-5">
          <h2 class="section-title">The CSS</h2>
          <p class="section-subtitle fw-light mt-2 mb-5" style="margin-bottom:1rem;margin-top:0.9rem;">Everything you need — just four lines of meaningful CSS plus vendor prefixes for Safari.</p>
          <p style="color:var(--text-muted);font-size:0.9rem;line-height:1.75;font-weight:300">Scroll up and down this page and watch the navbar blur the coloured orbs behind it in real time. On scroll, a slightly darker tint is applied for better readability.</p>
        </div>
        <div class="col-lg-7">
          <div class="code-glass">
            <div class="dot-row d-flex gap-2 mb-4"><span class="dot r"></span><span class="dot y"></span><span class="dot g"></span></div>
            <span class="sel">.navbar-glass</span> {<br>
            &nbsp;&nbsp;<span class="prop">background</span>: <span class="val">rgba(255, 255, 255, 0.12)</span>;<br>
            &nbsp;&nbsp;<span class="prop">backdrop-filter</span>: <span class="val">blur(20px) saturate(180%)</span>;<br>
            &nbsp;&nbsp;<span class="prop">-webkit-backdrop-filter</span>: <span class="val">blur(20px) saturate(180%)</span>;<br>
            &nbsp;&nbsp;<span class="prop">border-bottom</span>: <span class="val">1px solid rgba(255, 255, 255, 0.18)</span>;<br>
            &nbsp;&nbsp;<span class="prop">box-shadow</span>: <span class="val">0 8px 32px rgba(0, 0, 0, 0.25)</span>;<br>
            }<br><br>
            <span class="cmt">/* Darken on scroll for readability */</span><br>
            <span class="sel">.navbar-glass.scrolled</span> {<br>
            &nbsp;&nbsp;<span class="prop">background</span>: <span class="val">rgba(8, 8, 22, 0.65)</span>;<br>
            }
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Showcase -->
  <section class="section" id="cards">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="section-title">Glass Everywhere</h2>
        <p class="section-subtitle fw-light mt-2 mb-5">The same technique applied to cards, modals, and more</p>
      </div>
      <div class="row g-4">
        <div class="col-md-6 col-lg-3 fade-up">
          <div class="glass-card text-center">
            <div style="font-size:2.25rem;margin-bottom:0.7rem">🎨</div>
            <h5 class="mb-2">Theming</h5>
            <p class="fw-light lh-lg">Swap CSS variables to adapt the glass tint to any brand palette instantly.</p>
          </div>
        </div>
        <div class="col-md-6 col-lg-3 fade-up">
          <div class="glass-card text-center">
            <div style="font-size:2.25rem;margin-bottom:0.7rem">📱</div>
            <h5 class="mb-2">Responsive</h5>
            <p class="fw-light lh-lg">Works beautifully on mobile with a collapsible menu that inherits the glass effect.</p>
          </div>
        </div>
        <div class="col-md-6 col-lg-3 fade-up">
          <div class="glass-card text-center">
            <div style="font-size:2.25rem;margin-bottom:0.7rem">⚡</div>
            <h5 class="mb-2">Performant</h5>
            <p class="fw-light lh-lg">Hardware-accelerated via the GPU compositor — smooth even on mid-range devices.</p>
          </div>
        </div>
        <div class="col-md-6 col-lg-3 fade-up">
          <div class="glass-card text-center">
            <div style="font-size:2.25rem;margin-bottom:0.7rem">🌐</div>
            <h5 class="mb-2">Browser Support</h5>
            <p class="fw-light lh-lg">Supported in all modern browsers. Safari needs the <code>-webkit-</code> prefix.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <section class="section" id="testimonials">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="section-title">What Developers Say</h2>
        <p class="section-subtitle fw-light mt-2 mb-5">Glassmorphism in production — from teams around the world</p>
      </div>
      <div class="row g-4 align-items-start">

        <div class="col-12 col-md-6 col-xl-3 fade-up">
          <div class="testimonial-card" data-company="Vercel" data-name="Sarah Mitchell" data-role="Senior Frontend Engineer" data-quote="We adopted glassmorphism for our dashboard redesign and the results were remarkable. The frosted-glass panels let the data visualisations breathe while keeping the UI feeling cohesive. backdrop-filter turned a flat interface into something that feels genuinely tactile — our users immediately noticed the difference.">
            <span class="t-quote-mark" aria-hidden="true">&ldquo;</span>
            <p class="testimonial-body">We adopted glassmorphism for our dashboard redesign and the results were remarkable. The frosted-glass panels let the data visualisations breathe while keeping the UI feeling cohesive. backdrop-filter turned a flat interface into something that feels genuinely tactile.</p>
            <button class="btn-read-more" tabindex="-1" aria-hidden="true">Read in full →</button>
            <hr class="t-divider border-0 border-top my-0 mb-4">
            <p class="t-company">Vercel</p>
            <p class="t-name">Sarah Mitchell<br>Senior Frontend Engineer</p>
          </div>
        </div>

        <div class="col-12 col-md-6 col-xl-3 fade-up">
          <div class="testimonial-card" data-company="Stripe" data-name="James Okafor" data-role="UI Design Lead" data-quote="The glass effect strikes a perfect balance between visual richness and performance. We were initially worried about GPU overhead on lower-end devices, but hardware acceleration meant it ran smoothly across our entire user base. Combining it with subtle animated gradients in the background elevated the entire product aesthetic.">
            <span class="t-quote-mark" aria-hidden="true">&ldquo;</span>
            <p class="testimonial-body">The glass effect strikes a perfect balance between visual richness and performance. We were initially worried about GPU overhead on lower-end devices, but hardware acceleration meant it ran smoothly across our entire user base.</p>
            <button class="btn-read-more" tabindex="-1" aria-hidden="true">Read in full →</button>
            <hr class="t-divider border-0 border-top my-0 mb-4">
            <p class="t-company">Stripe</p>
            <p class="t-name">James Okafor<br>UI Design Lead</p>
          </div>
        </div>

        <div class="col-12 col-md-6 col-xl-3 fade-up">
          <div class="testimonial-card" data-company="Linear" data-name="Priya Nair" data-role="Product Designer" data-quote="Glassmorphism gave our project management tool a calm, focused atmosphere. When you're staring at a screen all day, the softness of blurred layers reduces visual fatigue compared to hard-edged cards. It is one of those subtle choices that users rarely consciously notice — but definitely feel.">
            <span class="t-quote-mark" aria-hidden="true">&ldquo;</span>
            <p class="testimonial-body">Glassmorphism gave our project management tool a calm, focused atmosphere. When you're staring at a screen all day, the softness of blurred layers reduces visual fatigue compared to hard-edged cards.</p>
            <button class="btn-read-more" tabindex="-1" aria-hidden="true">Read in full →</button>
            <hr class="t-divider border-0 border-top my-0 mb-4">
            <p class="t-company">Linear</p>
            <p class="t-name">Priya Nair<br>Product Designer</p>
          </div>
        </div>

        <div class="col-12 col-md-6 col-xl-3 fade-up">
          <div class="testimonial-card" data-company="Figma" data-name="Tom Eriksson" data-role="Creative Technologist" data-quote="What I love most about this technique is how it democratises depth. Previously you needed complex drop shadows or gradients to create layering. Now four lines of CSS — background, backdrop-filter, border, and box-shadow — achieve a sense of physical space that used to take hours to design. It fits naturally into design systems and scales incredibly well from small components to full-page layouts.">
            <span class="t-quote-mark" aria-hidden="true">&ldquo;</span>
            <p class="testimonial-body">What I love most about this technique is how it democratises depth. Previously you needed complex drop shadows or gradients to create layering. Now four lines of CSS achieve a sense of physical space that used to take hours to design.</p>
            <button class="btn-read-more" tabindex="-1" aria-hidden="true">Read in full →</button>
            <hr class="t-divider border-0 border-top my-0 mb-4">
            <p class="t-company">Figma</p>
            <p class="t-name">Tom Eriksson<br>Creative Technologist</p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <footer class="py-5 text-center">
    <div class="container">
      Glassmorphism Demo &middot; Built with Bootstrap 5 &middot; No JS frameworks required
    </div>
  </footer>
</div>

<!-- Testimonial modal -->
<div class="modal fade" id="testimonialModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-md">
    <div class="modal-content">
      <div class="modal-header">
        <div>
          <p class="modal-company mb-1" id="modal-company"></p>
          <p class="modal-person" id="modal-person"></p>
        </div>
        <button type="button" class="btn-close ms-3" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body p-4">
        <span class="t-quote-mark" aria-hidden="true">&ldquo;</span>
        <p class="modal-quote-body" id="modal-quote"></p>
      </div>
      <div class="modal-footer px-4 py-3">
        <button type="button" class="btn-close-modal rounded-2" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>




:root {
/*   Glassmorphism tokens */
  --glass-bg: rgba(255, 255, 255, 0.1);
  --glass-border: rgba(255, 255, 255, 0.18);
  --glass-shadow: rgba(0, 0, 0, 0.25);
  --accent: #7df9ff;
  --accent-warm: #ff6b9d;
  --accent-purple: #a78bfa;
  --text-muted-alpha: 0.58;

/*   Bootstrap overrides */
  --bs-body-font-family: "Epilogue", sans-serif;
  --bs-body-bg: #080816;
  --bs-body-color: #f0f0f0;
  --bs-body-color-rgb: 240, 240, 240;

}
/* Navbar */
  .navbar {
    --bs-navbar-color: rgba(255, 255, 255, 0.58);
    --bs-navbar-hover-color: #fff;
    --bs-navbar-active-color: #fff;
    --bs-navbar-brand-font-size: 1.45rem;
    --bs-navbar-brand-color: var(--accent);
    --bs-navbar-brand-hover-color: var(--accent);
    --bs-navbar-toggler-border-color: var(--glass-border);
    --bs-navbar-toggler-icon-bg: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba(255,255,255,1)' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
  }
/* Nav links */
  .navbar-nav {
    --bs-nav-link-font-size: 0.9rem;
    --bs-nav-link-font-weight: 400;
    --bs-nav-link-color: rgba(255, 255, 255, 0.58);
    --bs-nav-link-hover-color: #fff;
  }
/* Modal */
  .modal {
    --bs-modal-bg: rgba(8, 8, 22, 0.88);
    --bs-modal-color: #fff;
    --bs-modal-border-color: var(--glass-border);
    --bs-modal-border-radius: 22px;
    --bs-modal-header-border-color: rgba(255, 255, 255, 0.07);
    --bs-modal-footer-border-color: rgba(255, 255, 255, 0.07);
    --bs-modal-header-padding: 1.5rem 1.75rem 1.2rem;
    --bs-modal-inner-border-radius: 22px;
    --bs-backdrop-opacity: 0.78;
  }
body {
  overflow-x: hidden;
}

/* ─── Background ─── */
.bg-scene {
  position: fixed;
  inset: 0;
  z-index: 0;
  overflow: hidden;
}
.bg-scene::before {
  content: "";
  position: absolute;
  width: 140vmax;
  height: 140vmax;
  top: -40%;
  left: -20%;
  background: conic-gradient(
    from 180deg,
    #1a0533 0deg,
    #0d1f4c 60deg,
    #0a2a3c 120deg,
    #1a0533 180deg,
    #2d1b4e 240deg,
    #0d1f4c 300deg,
    #1a0533 360deg
  );
  animation: rotateBg 40s linear infinite;
}
@keyframes rotateBg {
  to {
    transform: rotate(360deg);
  }
}

.orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(80px);
  opacity: 0.45;
  animation: drift 20s ease-in-out infinite alternate;
}
.orb-1 {
  width: 500px;
  height: 500px;
  background: radial-gradient(circle, #7df9ff 0%, transparent 70%);
  top: 10%;
  left: 60%;
  animation-duration: 18s;
}
.orb-2 {
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, #ff6b9d 0%, transparent 70%);
  top: 50%;
  left: 10%;
  animation-duration: 22s;
  animation-delay: -5s;
}
.orb-3 {
  width: 350px;
  height: 350px;
  background: radial-gradient(circle, #a78bfa 0%, transparent 70%);
  top: 70%;
  left: 70%;
  animation-duration: 25s;
  animation-delay: -10s;
}

@keyframes drift {
  0% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(60px, -40px) scale(1.1);
  }
  66% {
    transform: translate(-40px, 30px) scale(0.95);
  }
  100% {
    transform: translate(30px, -60px) scale(1.05);
  }
}

/* ─── Navbar ─── */
.navbar-glass {
  background: var(--glass-bg) !important;
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border-bottom: 1px solid var(--glass-border);
  box-shadow: 0 8px 32px var(--glass-shadow);
  transition: background 0.3s ease, box-shadow 0.3s ease;
}
.navbar-glass.scrolled {
  background: rgba(8, 8, 22, 0.65) !important;
  box-shadow: 0 12px 48px rgba(0, 0, 0, 0.45);
}
.navbar-brand {
  font-family: "Syne", sans-serif;
  font-weight: 800;
  letter-spacing: -0.01em;
  background: linear-gradient(135deg, var(--accent), var(--accent-warm));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.navbar .nav-link {
    /* Nav links */
  --bs-nav-link-font-size: 0.9rem;
  --bs-nav-link-font-weight: 400;
  --bs-nav-link-color: rgba(255, 255, 255, 0.58);
  --bs-nav-link-hover-color: #fff;
  --bs-navbar-active-color: #fff;
}
.navbar .nav-link {
  font-family: "Epilogue", sans-serif;
  letter-spacing: 0.01em;
  padding: 0.45rem 0.9rem;
  border-radius: 8px;
  transition: all 0.22s ease;
  position: relative;
}
.navbar .nav-link:hover,
.navbar .nav-link.active {
  background: rgba(255, 255, 255, 0.08);
}
.navbar .nav-link.active::after {
  content: "";
  position: absolute;
  bottom: 3px;
  left: 50%;
  transform: translateX(-50%);
  width: 18px;
  height: 2px;
  background: var(--accent);
  border-radius: 1px;
}
.btn-glass {
  background: rgba(125, 249, 255, 0.12);
  border: 1px solid rgba(125, 249, 255, 0.28);
  color: var(--accent);
  backdrop-filter: blur(10px);
  border-radius: 10px;
  padding: 0.42rem 1.2rem;
  font-weight: 500;
  font-size: 0.88rem;
  transition: all 0.22s ease;
  text-decoration: none;
  display: inline-block;
  font-family: "Epilogue", sans-serif;
  letter-spacing: 0.02em;
}
.btn-glass:hover {
  background: rgba(125, 249, 255, 0.22);
  color: #fff;
  box-shadow: 0 0 22px rgba(125, 249, 255, 0.18);
  transform: translateY(-1px);
}
.navbar-toggler {
  padding: 0.4rem 0.6rem;
}

/* ─── Content wrapper ─── */
.content-wrapper {
  z-index: 1;
}

/* ─── GLASS HEADLINE ─── */
.headline-glass {
  display: inline-block;
  position: relative;
  padding: 1.1rem 2rem 1.25rem;
  border-radius: 20px;
  background: rgba(255, 255, 255, 0.07);
  backdrop-filter: blur(18px) saturate(160%);
  -webkit-backdrop-filter: blur(18px) saturate(160%);
  border: 1px solid rgba(255, 255, 255, 0.14);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.15), inset 0 -1px 0 rgba(0, 0, 0, 0.2);
  transform-style: preserve-3d;
  will-change: transform;
  cursor: default;
  transition: box-shadow 0.25s ease;
}
.headline-glass:hover {
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.45),
    0 0 40px rgba(125, 249, 255, 0.08), inset 0 1px 0 rgba(255, 255, 255, 0.2),
    inset 0 -1px 0 rgba(0, 0, 0, 0.2);
}
.headline-glass::after {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.12) 0%,
    transparent 50%,
    rgba(125, 249, 255, 0.05) 100%
  );
  pointer-events: none;
  transition: opacity 0.3s;
}

/* Section-level headline glass (centered) */
.headline-glass-wrap {
  perspective: 600px;
}
.headline-glass-wrap.left {
  justify-content: flex-start;
}

/* ─── Typography ─── */
.display-headline {
  font-family: "Syne", sans-serif;
  font-weight: 800;
}
h2.section-title {
  font-family: "Syne", sans-serif;
  font-weight: 800;
  font-size: clamp(1.8rem, 3vw, 2.6rem);
  line-height: 1.1;
  letter-spacing: -0.03em;
  color: #fff;
  margin: 0;
}
.section-subtitle {
  font-family: "Epilogue", sans-serif;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  font-size: 1rem;
  letter-spacing: 0.01em;
}

/* ─── Hero ─── */
.hero {
  padding: 9rem 1rem 5rem;
}
.hero-sub {
  font-size: 1.1rem;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  max-width: 500px;
  line-height: 1.75;
}
.hero .btn-glass {
  font-size: 0.95rem;
  padding: 0.65rem 2rem;
  margin-top: 0.25rem;
}

/* ─── Sections ─── */
.section {
  padding: 6rem 0;
}

/* ─── Glass Cards ─── */
.glass-card {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px) saturate(160%);
  -webkit-backdrop-filter: blur(16px) saturate(160%);
  border: 1px solid var(--glass-border);
  border-radius: 20px;
  padding: 2.25rem;
  transition: transform 0.32s ease, box-shadow 0.32s ease;
  height: 100%;
}
.glass-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 22px 60px rgba(0, 0, 0, 0.32);
}
.card-icon {
  width: 52px;
  height: 52px;
  border-radius: 14px;
  font-size: 1.4rem;
}
.card-icon.cyan {
  background: rgba(125, 249, 255, 0.13);
  color: var(--accent);
}
.card-icon.pink {
  background: rgba(255, 107, 157, 0.13);
  color: var(--accent-warm);
}
.card-icon.purple {
  background: rgba(167, 139, 250, 0.13);
  color: var(--accent-purple);
}
.glass-card h5 {
  font-family: "Syne", sans-serif;
  font-weight: 700;
  font-size: 1.05rem;
  letter-spacing: -0.01em;
}
.glass-card p {
  color: rgba(var(--bs-body-color-rgb), 0.58);
  font-size: 0.9rem;
  margin: 0;
}

/* ─── Code Block ─── */
.code-glass {
  background: rgba(0, 0, 0, 0.38);
  backdrop-filter: blur(12px);
  border: 1px solid var(--glass-border);
  border-radius: 16px;
  padding: 2rem;
  font-family: "SF Mono", "Fira Code", monospace;
  font-size: 0.83rem;
  line-height: 1.8;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  overflow-x: auto;
}
.code-glass .prop {
  color: var(--accent);
}
.code-glass .val {
  color: var(--accent-warm);
}
.code-glass .sel {
  color: var(--accent-purple);
}
.code-glass .cmt {
  color: rgba(255, 255, 255, 0.22);
  font-style: italic;
}
.dot-row .dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}
.dot.r { background: #ff5f57; }
.dot.y { background: #febc2e; }
.dot.g { background: #28c840; }

/* ─── Testimonials ─── */
.testimonial-card {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px) saturate(160%);
  -webkit-backdrop-filter: blur(16px) saturate(160%);
  border: 1px solid var(--glass-border);
  border-radius: 20px;
  padding: 2rem 2rem 1.75rem;
  display: flex;
  flex-direction: column;
  cursor: pointer;
  transition: border-color 0.25s ease, transform 0.32s ease,
    box-shadow 0.32s ease;
  position: relative;
  overflow: hidden;
}
.testimonial-card::before {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(
    circle at top left,
    rgba(125, 249, 255, 0.07),
    transparent 60%
  );
  opacity: 0;
  transition: opacity 0.35s ease;
  pointer-events: none;
}
.testimonial-card:hover {
  border-color: rgba(125, 249, 255, 0.28);
  transform: translateY(-5px);
  box-shadow: 0 18px 50px rgba(0, 0, 0, 0.3);
}
.testimonial-card:hover::before {
  opacity: 1;
}
.t-quote-mark {
  font-family: "Syne", sans-serif;
  font-weight: 800;
  font-size: 5rem;
  line-height: 1;
  color: var(--accent);
  opacity: 0.45;
  margin-bottom: -1.5rem;
  display: block;
  user-select: none;
}
.testimonial-body {
  font-size: 0.9rem;
  font-weight: 300;
  line-height: 1.75;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  flex: 1;
  margin-bottom: 1.25rem;
  display: -webkit-box;
  -webkit-line-clamp: 5;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.btn-read-more {
  background: none;
  border: none;
  padding: 0;
  font-size: 0.76rem;
  font-weight: 500;
  letter-spacing: 0.07em;
  color: var(--accent);
  cursor: pointer;
  margin-bottom: 1.25rem;
  display: none;
  text-align: left;
  font-family: "Epilogue", sans-serif;
  transition: opacity 0.2s;
}
.btn-read-more:hover {
  opacity: 0.7;
}
.t-divider {
  border-color: rgba(255, 255, 255, 0.1);
}
.t-company {
  font-family: "Syne", sans-serif;
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 0.2rem;
}
.t-name {
  font-size: 0.8rem;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  margin: 0;
  font-weight: 300;
}

/* ─── Modal ─── */
.modal-content {
  backdrop-filter: blur(28px) saturate(180%);
  -webkit-backdrop-filter: blur(28px) saturate(180%);
}
.modal .t-quote-mark {
  font-size: 5.5rem;
  margin-bottom: -1.75rem;
}
.modal-quote-body {
  font-family: "Syne", sans-serif;
  font-weight: 700;
  font-size: clamp(1rem, 2vw, 1.15rem);
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.88);
  margin-bottom: 0;
  letter-spacing: -0.01em;
}
.modal-company {
  font-family: "Syne", sans-serif;
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--accent);
}
.modal-person {
  font-size: 0.85rem;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  margin: 0;
  line-height: 1.5;
  font-weight: 300;
}
.btn-close-modal {
  background: rgba(125, 249, 255, 0.09);
  border: 1px solid rgba(125, 249, 255, 0.22);
  color: var(--accent);
  font-size: 0.8rem;
  padding: 0.38rem 1.1rem;
  cursor: pointer;
  font-family: "Epilogue", sans-serif;
  font-weight: 500;
  transition: all 0.2s;
  letter-spacing: 0.04em;
}
.btn-close-modal:hover {
  background: rgba(125, 249, 255, 0.18);
  color: #fff;
}
.modal-header .btn-close {
  --bs-btn-close-color: #fff;
  --bs-btn-close-opacity: 0.55;
  --bs-btn-close-hover-opacity: 1;
  filter: invert(1) grayscale(1) brightness(1.5);
}

/* ─── Footer ─── */
footer {
  font-size: 0.82rem;
  color: rgba(var(--bs-body-color-rgb), 0.58);
  border-top: 1px solid rgba(255, 255, 255, 0.05);
  font-weight: 300;
  letter-spacing: 0.03em;
}

/* ─── Fade-up ─── */
.fade-up {
  opacity: 0;
  transform: translateY(28px);
  animation: fadeUp 0.75s ease forwards;
}
.fade-up:nth-child(2) { animation-delay: 0.13s; }
.fade-up:nth-child(3) { animation-delay: 0.26s; }
.fade-up:nth-child(4) { animation-delay: 0.39s; }
@keyframes fadeUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}


/* ── Scrolled navbar ── */
const nav = document.getElementById("mainNav");
window.addEventListener("scroll", () => {
  nav.classList.toggle("scrolled", window.scrollY > 40);
});

/* ── Fade-up on scroll ── */
const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach((e) => {
      if (e.isIntersecting) {
        e.target.style.animationPlayState = "running";
        observer.unobserve(e.target);
      }
    });
  },
  { threshold: 0.15 }
);
document.querySelectorAll(".fade-up").forEach((el) => {
  el.style.animationPlayState = "paused";
  observer.observe(el);
});

/* ── 3D tilt on headlines ── */
const MAX_TILT = 12; // degrees
const MAX_LIFT = 14; // px translateZ

document.querySelectorAll(".tilt-3d").forEach((el) => {
  const wrap = el.parentElement;
  // Set perspective on the wrapper
  wrap.style.perspective = "700px";

  el.addEventListener("mousemove", (e) => {
    const r = el.getBoundingClientRect();
    const cx = r.left + r.width / 2;
    const cy = r.top + r.height / 2;
    const dx = (e.clientX - cx) / (r.width / 2); // -1 → +1
    const dy = (e.clientY - cy) / (r.height / 2); // -1 → +1
    const rotY = dx * MAX_TILT;
    const rotX = -dy * MAX_TILT;
    el.style.transition = "transform 0.08s linear, box-shadow 0.25s ease";
    el.style.transform = `rotateX(${rotX}deg) rotateY(${rotY}deg) translateZ(${MAX_LIFT}px)`;
  });

  el.addEventListener("mouseleave", () => {
    el.style.transition =
      "transform 0.55s cubic-bezier(0.23,1,0.32,1), box-shadow 0.25s ease";
    el.style.transform = "rotateX(0deg) rotateY(0deg) translateZ(0px)";
  });
});

/* ── Testimonial modal ── */
const bsModal = new bootstrap.Modal(
  document.getElementById("testimonialModal")
);
const modalCo = document.getElementById("modal-company");
const modalPerson = document.getElementById("modal-person");
const modalQuote = document.getElementById("modal-quote");

document.querySelectorAll(".testimonial-card").forEach((card) => {
  const body = card.querySelector(".testimonial-body");
  const btn = card.querySelector(".btn-read-more");
  if (body.scrollHeight > body.clientHeight + 2) {
    btn.style.display = "inline-block";
    btn.removeAttribute("aria-hidden");
    btn.removeAttribute("tabindex");
  }
  card.addEventListener("click", () => {
    modalCo.textContent = card.dataset.company;
    modalPerson.innerHTML = `${card.dataset.name} &mdash; <span style="opacity:0.65">${card.dataset.role}</span>`;
    modalQuote.textContent = card.dataset.quote;
    bsModal.show();
  });
});

